<div class="header">
    <div class="content">
        <a class="logo" href="{{ base }}"></a>
        <div class="toolbar">
            <a href="{{ base }}" {{ (pathElements[0] == "index")|addClass("active") }}>Лента</a>
            <div class="separator"></div>
            <a href="{{ base }}files" {{ (pathElements[0] == "files")|addClass("active") }}>Файлы</a>
            <div class="separator"></div>
            <a href="{{ base }}me" {{ (pathElements[0] == "profile")|addClass("active") }}>Профиль</a>
        </div>
    </div>
</div>