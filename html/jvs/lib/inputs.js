/* Object to work with inputs */
sky.inputs = {

	/* Classes */
	classes : {
		error	: "error",
		notice	: "notice",
		info  	: "info",
		success	: "success"
	},
	
	/**
	 * Marks input
	 * @param {object}	object	Input to mark
	 * @param {text}	text	Hint text
	 * @param {type}	type	Mark type
	 */
	mark: function(object, text, type) {

	},
	
	/**
	 * Un mark marked field
	 */
	unmark: function(object) {
		
	}
	
};